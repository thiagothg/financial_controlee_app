import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:financialcontroleeapp/app/modules/todo_module.dart';

void main() {
  initModule(TodoModule());
  // TodoController todo;
  //
  setUp(() {
    //     todo = TodoModule.to.get<TodoController>();
  });

  group('TodoController Test', () {
    //   test("First Test", () {
    //     expect(todo, isInstanceOf<TodoController>());
    //   });

    //   test("Set Value", () {
    //     expect(todo.value, equals(0));
    //     todo.increment();
    //     expect(todo.value, equals(1));
    //   });
  });
}
