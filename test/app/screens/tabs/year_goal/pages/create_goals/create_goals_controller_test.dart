import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:financialcontroleeapp/app/app_module.dart';

void main() {
  initModule(AppModule());
  // CreateGoalsController creategoals;
  //
  setUp(() {
    //     creategoals = AppModule.to.get<CreateGoalsController>();
  });

  group('CreateGoalsController Test', () {
    //   test("First Test", () {
    //     expect(creategoals, isInstanceOf<CreateGoalsController>());
    //   });

    //   test("Set Value", () {
    //     expect(creategoals.value, equals(0));
    //     creategoals.increment();
    //     expect(creategoals.value, equals(1));
    //   });
  });
}
