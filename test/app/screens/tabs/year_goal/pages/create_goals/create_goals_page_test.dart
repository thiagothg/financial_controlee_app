import 'package:flutter_test/flutter_test.dart';


void main() {
  testWidgets('CreateGoalsPage has title', (tester) async {
    // ignore: lines_longer_than_80_chars
    //  await tester.pumpWidget(buildTestableWidget(CreateGoalsPage(title: 'CreateGoals')));
    //  final titleFinder = find.text('CreateGoals');
    //  expect(titleFinder, findsOneWidget);
  });
}
