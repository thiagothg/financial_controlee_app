import 'package:flutter_test/flutter_test.dart';


void main() {
  testWidgets('GoalsPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(GoalsPage(title: 'Goals')));
    //  final titleFinder = find.text('Goals');
    //  expect(titleFinder, findsOneWidget);
  });
}
